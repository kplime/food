# For local dev, leave unset — communityStore.ts/authStore.ts default to
# http://127.0.0.1:8000/api. Set this in Vercel's project settings once the
# Render backend is deployed, e.g.:
VITE_API_BASE=https://obang-korean-food-backend.onrender.com/api
